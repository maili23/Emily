<p id="notice"><%= notice %></p>

<h1>Listed Products</h1>

<!-- The old Table just for reference issues
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Color</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.image_url %></td>
        <td><%= product.color %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<div class="container">
    <div class="row">
      <% @products.each do |product| %>
        <div class="col-sm-2 col-md-4">
          <a href="#"><h1><%= product.name %></h1></a>
          <%= image_tag(product.image_url, class: "img-responsive") %>
          <p><%= product.description %></p>
          <p><%= product.color %></p>
          <p><%= product.price %></p>
          <p><%= link_to 'Show', product %> <%= link_to 'Edit', edit_product_path(product) %> <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
        </div><!-- /col -->
      <% end %>
    </div>    
  </div>



<br>

<%= link_to 'New Product', new_product_path %>


<!-- This is just an example of writing a loop with ERB Code
<% paragraph_number = 1 %>
<% 5.times do %>
  <p>This is a paragraph number <%= paragraph_number %></p>
  <% paragraph_number = paragraph_number + 1 %>
<% end %> -->

